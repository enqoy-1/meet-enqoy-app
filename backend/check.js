const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.user.findUnique({ where: { email: 'admin@enqoy.com' }, include: { roles: true } }).then(u => console.log(JSON.stringify(u, null, 2)) || prisma.\()).catch(e => console.error(e) || prisma.\());
